# Mobile Setup

현재 환경에서 `flutter` CLI가 설치되어 있지 않아 자동 초기화는 진행되지 않았습니다.

## Required
1. Flutter SDK 설치
2. PATH에 `flutter` 등록
3. 아래 명령 실행

```bash
cd apps/mobile
flutter create .
flutter pub get
flutter run
```
